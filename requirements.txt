# --- Core --------------------------------------------------------------
python-dotenv>=1.0
APScheduler>=3.10
loguru>=0.7

# --- Browser automation -----------------------------------------------
playwright>=1.48

# --- AI ---------------------------------------------------------------
anthropic>=0.39
openai-whisper>=20240930
# torch is left unpinned so pip can pick a wheel that matches the
# installed Python version (Python 3.14 needs torch >= 2.5).
torch

# --- Video / download -------------------------------------------------
# yt-dlp gets weekly patches as YouTube changes its anti-bot challenges
# (SABR streaming, PO Tokens, etc.).  Pin to a recent floor and let pip
# pick the latest available so cloud-IP downloads keep working.
yt-dlp>=2026.05.20
ffmpeg-python>=0.2

# --- Platform SDKs ----------------------------------------------------
google-api-python-client>=2.149
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2
requests>=2.32

# --- Utility ----------------------------------------------------------
pillow>=10.4
pydantic>=2.9
tenacity>=9.0
python-telegram-bot>=21.6
